#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_remarkable@1.7.4@remarkable/bin/remarkable.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../_remarkable@1.7.4@remarkable/bin/remarkable.js" "$@"
  ret=$?
fi
exit $ret
